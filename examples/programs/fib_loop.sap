defineFunction fib(n)
    set a = 0
    set b = 1
    set counter = 0
    repeat until counter == n
        set counter = counter + 1
        set temp = a
        set a = b
        set b = temp+b
    end
    return a
end

fib(30)

